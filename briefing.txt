====================================================
  LUXEDRIVE — PROJECT BRIEFING
  Last updated: July 18, 2026
====================================================

PROJECT OVERVIEW
----------------
Name: LuxeDrive
Type: Car dealership management system (gestion de voitures)
Language: Python (Flask) + SQLite + Jinja2 HTML templates
Market: Moroccan (prices in MAD, Moroccan plate format)
UI Language: French
Theme: Dark UI — black background (#050505), red primary (#990000), gold accent (#D4AF37)

The app runs LOCALLY on the user's PC. It is NOT deployed online.
To run: activate venv, then `python app.py`. Browser opens automatically at http://127.0.0.1:5000

GitHub repo: https://github.com/imadmonyr/PRROJECT_IMAD
Note: GitHub only contains a PARTIAL copy of the project (the key files).
The FULL project lives locally at:
  C:\Users\PC\Desktop\imad docc projet fin d'anner\gestion-voiture\


HOW TO RUN
----------
1. Open Command Prompt (cmd) — NOT PowerShell (PowerShell blocks venv activation)
2. Navigate to project folder:
   cd "C:\Users\PC\Desktop\imad docc projet fin d'anner\gestion-voiture"
3. Activate virtual environment:
   venv\Scripts\activate
4. Run the app:
   python app.py
5. Browser opens automatically at http://127.0.0.1:5000


DATABASE
--------
File: database.db (located in the root project folder)
Full path: C:\Users\PC\Desktop\imad docc projet fin d'anner\gestion-voiture\database.db
Engine: SQLite
Viewer tool: DB Browser for SQLite (sqlitebrowser.org) — open database.db to browse/edit data
  OR: SQLite Viewer extension in VS Code

The app writes directly to this file. Any car/client/sale added through the website
is saved here instantly.


DATABASE SCHEMA (tables)
------------------------
- marque          → car brands
- modele          → car models (linked to marque)  ← MODIFIED (see below)
- voiture         → cars (VIN, model, type, plate, year, buy price, sell price, status)
- client          → customers
- fournisseur     → suppliers
- agent           → sales agents (with commission rate %)
- commande_achat  → purchase orders (from suppliers)
- ligne_achat     → cars per purchase order (links commande_achat → voiture by VIN)
- vente           → sales transactions
- ligne_vente     → cars per sale (links vente → voiture by VIN)
- facture         → invoices
- paiement        → payments
- historique_proprietaire → ownership history
- user            → app users (roles: admin / user)

User roles:
  admin → full access to everything
  user  → browse all brands, see available cars, request a devis (quote)


⚠ DATABASE CHANGES MADE BY AGENT (IMPORTANT FOR MCD/MLD)
----------------------------------------------------------
The following changes were made to the database structure and data.
These changes affect your MCD (Modèle Conceptuel de Données) and MLD
(Modèle Logique de Données) — update them accordingly.

1. NEW COLUMN ADDED: modele.image_url
   Table:  modele
   Column: image_url TEXT (nullable)
   Purpose: Stores a URL to a real car photo, displayed on the brand detail page.
   MCD impact: The "Modèle" entity now has an additional attribute: image_url (URL)
   MLD impact: modele(id_modele, nom_modele, id_marque, image_url)
               was: modele(id_modele, nom_modele, id_marque)

2. NEW MODELS INSERTED (24 new rows in the modele table)
   The original seed data had 19 models across 8 brands.
   The agent added 3 new models per brand = 24 additional models.
   Total after update: 43 models.

   New models added per brand:
   ┌────────────────┬──────────────────────────────────────────┐
   │ Brand          │ New Models Added                         │
   ├────────────────┼──────────────────────────────────────────┤
   │ Mercedes-Benz  │ EQS 580 4MATIC, Classe C 300d, GLC 300  │
   │ BMW            │ M5 G90, i7 xDrive60, X5 M Competition   │
   │ Audi           │ R8 V10 Plus, e-tron GT RS, SQ8 TFSI     │
   │ Porsche        │ Taycan Turbo S, Macan GTS, 718 Spyder RS │
   │ Ferrari        │ SF90 XX Stradale, 296 GTB, Roma Spider   │
   │ Lamborghini    │ Revuelto, Urus SE, Huracán Sterrato      │
   │ Bentley        │ Continental GT Speed, Bentayga EWB, Flying Spur S │
   │ Rolls-Royce    │ Spectre, Ghost Black Badge, Cullinan Series II │
   └────────────────┴──────────────────────────────────────────┘


FEATURES IMPLEMENTED
--------------------
The following features have been fully implemented. This list is the source of truth
for what the app can and cannot do.

✅ 1. Edit & Delete cars
   Routes: /voitures/edit/<vin>  (GET/POST)
           /voitures/delete/<vin> (POST)
   Files:  templates/voiture_edit.html, app.py

✅ 2. Search & filter on voitures list (text search)
   Filter by: brand, model, plate, VIN (text search)
              status chips: Tous / En Stock / Réservées / Vendues
              type chips:   Tous types / Neuf / Occasion
   Implementation: client-side JS in templates/voitures.html
   No backend change needed.

✅ 3. Client "Mes Devis" page
   Route: /client/mes-devis
   Shows the logged-in client's full quote history (date, vehicle, price HT/TTC, status).
   Devis are saved to the devis table on each request.
   Files: templates/mes_devis.html, app.py

✅ 4. Edit & Delete clients, agents, suppliers
   Routes: /clients/edit/<id>       /clients/delete/<id>
           /agents/edit/<id>        /agents/delete/<id>
           /fournisseurs/edit/<id>  /fournisseurs/delete/<id>
   Files:  templates/client_edit.html, templates/agent_edit.html,
           templates/fournisseur_edit.html, app.py

✅ 5. Monthly sales chart on dashboard
   Dashboard fetches last 12 months of sales (count + revenue per month).
   Data passed to index.html as JSON (chart_labels, chart_ventes, chart_ca).
   Rendered with Chart.js canvas.
   Files: templates/index.html, app.py (dashboard route)

✅ 6. Car detail page
   Route: /voitures/<vin>
   Shows full car info. Admin sees Edit / Delete buttons.
   Files: templates/voiture_detail.html, app.py

✅ 7. Filter cars by price range, year, and type
   Price range: Min / Max MAD number inputs (filters as you type)
   Year range:  De / À number inputs (filters as you type)
   Type:        Neuf / Occasion chips (already existed)
   Reset button clears all filters at once.
   Implementation: client-side JS — no backend change, no Python change.
   All filters combine: search + status + type + price range + year range all work together.
   Files: templates/voitures.html
          (rows now carry data-prix and data-annee attributes)

✅ 8. My Account page for clients
   Route: /client/mon-compte  (GET/POST)
   Clients can view and update their name, phone, email.
   Files: templates/mon_compte.html, app.py

✅ 9. Print-ready invoice (facture) after a sale
   Route: /confirmation/<id_vente>
   Shown after admin records a sale. Includes facture data.
   Files: templates/confirmation.html, app.py

❌ Password hashing — NOT implemented (out of scope for local project).
   Passwords are stored and compared as plain text. Acceptable for a local school project.


CHANGELOG
---------
[July 2026 — Change 1] "Nouvel Achat" button fix + purchase order flow
  Problem: "Nouvel Achat" button in Ventes & Achats page was broken (href="#")
  Files changed:
    - templates/achat_add.html         (new file)
    - templates/transactions.html      (button href fixed)
    - app.py                           (added /achats/add route)
  DB impact: None (schema unchanged, just inserts into existing tables)

[July 2026 — Change 2] Real car photos on brand detail page
  Problem: Car cards showed a generic icon instead of a real photo
  Files changed:
    - app.py                           (marque_detail route: added m.image_url to SELECT)
    - templates/marque_detail.html     (replaced icon with <img> tag + fallback)
    - update_models.py                 (new migration script — run once locally)
  DB impact: ⚠ YES — see "DATABASE CHANGES MADE BY AGENT" section above

[July 2026 — Change 3] Full client browsing + devis flow
  Problem: Clients had no proper browsing experience; no quote system for them
  Files changed:
    - app.py                           (multiple updates — see below)
    - templates/marques.html           (hide admin controls from clients)
    - templates/marque_detail.html     (changed Acheter → Obtenir un Devis button)
    - templates/devis.html             (back button now context-aware)
  App.py changes:
    - /marques changed from @admin_required → @login_required (clients can now access it)
    - /marques/<id> now filters to en_stock only when role = 'user'
    - New route added: /client/devis/<vin> (client devis, no admin needed)
    - Login redirect for 'user' role: was /voitures → now /marques
    - Signup redirect: was /voitures → now /marques
  DB impact: None (reads existing data, no schema change)

[July 2026 — Change 4] Price range & year filters on voitures list
  Problem: No way to filter cars by price or year — list was unsearchable once large
  Files changed:
    - templates/voitures.html          (filter UI + updated JS applyFilters function)
  What was added:
    - Price range inputs (Min / Max MAD) inside the filter bar
    - Year range inputs (De / À) inside the filter bar
    - "Réinitialiser" button that clears all filters at once
    - data-prix and data-annee attributes on each <tr> row
    - Updated applyFilters() to combine all 6 filter dimensions simultaneously:
      text search + status chip + type chip + prix min + prix max + annee min + annee max
  App.py changes: None (purely client-side, no backend change needed)
  DB impact: None


TECH STACK
----------
Backend:  Python 3.x, Flask, SQLite (via sqlite3 module)
Frontend: Jinja2 templates, custom CSS (Inter font, Boxicons icons)
Auth:     Session-based (Flask sessions), role-based (admin/user)
Build:    Was previously compiled to .exe with PyInstaller (no longer needed)
Packages: Flask, and others listed in requirements.txt


AGENT NOTES
-----------
- Always use Command Prompt (cmd), NOT PowerShell — venv activation is blocked in PowerShell on this machine
- Database path in app.py is dynamic: resolves to same folder as app.py
- GitHub token needs to be provided by user for pushes (PAT tokens expire)
- The app auto-opens the browser on startup (webbrowser module in app.py)
- Moroccan license plate format is validated in the voitures_add form
- update_models.py must be run locally to apply DB changes (image_url column + new models)
- MCD/MLD need updating to reflect image_url column on the modele table
- All voitures.html filters are client-side JS — no Flask route changes needed for filtering
====================================================
